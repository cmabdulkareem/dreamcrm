NODE_ENV=development
PORT=3000
MONGODB_URI=your-mongodb-connection-string
JWT_SECRET=your-secret-key-here

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Option 1: SendGrid (Recommended for Production)
# Sign up at: https://signup.sendgrid.com/
# Create API key at: https://app.sendgrid.com/settings/api_keys
# Verify sender at: https://app.sendgrid.com/settings/sender_auth/senders
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# Option 2: Gmail SMTP (For Development)
# Generate App Password at: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_FROM=your-email@gmail.com

# ============================================
# NOTES
# ============================================
# 1. For production on Render/Heroku, use SendGrid (port 587/465 often blocked)
# 2. For local development, Gmail SMTP works fine
# 3. Frontend URL is auto-detected from CORS config (server/config/corsOptions.js)
# 4. Never commit the actual .env file to version control
