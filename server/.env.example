NODE_ENV=development
PORT=3000
MONGODB_URI=your-mongodb-connection-string
JWT_SECRET=your-secret-key-here

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Option 1: Resend (Recommended - Works everywhere!)
# Sign up at: https://resend.com/signup
# Get API key at: https://resend.com/api-keys
# Free tier: 100 emails/day, 3,000/month
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=your-email@gmail.com

# Option 2: SendGrid (Alternative)
# Sign up at: https://signup.sendgrid.com/
# Create API key at: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# Option 3: Gmail SMTP (For Development Only)
# Generate App Password at: https://myaccount.google.com/apppasswords
# Note: Often blocked on cloud platforms (Render, Heroku)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

# ============================================
# NOTES
# ============================================
# Priority: Resend > SendGrid > Gmail SMTP
# The service will automatically use the first available API key
# Frontend URL is auto-detected from CORS config (server/config/corsOptions.js)
# Never commit the actual .env file to version control
